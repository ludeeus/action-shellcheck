on: push
name: 'Trigger: Push'
jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Run Shellcheck
      uses: ./
      with:
        ignore: "ignore"
